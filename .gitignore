node_modules/
dist/
.DS_Store
*.log
.env
.env.local
*.tgz
coverage/
.vscode/
.idea/
test-results/
playwright-report/

# Pi session + audit + memory artifacts
audit.jsonl
session.jsonl
sessions/
*.sqlite
*.sqlite3
*.db

# Claude Code local artifacts — not shipped.
.claude/

# CI/CD workflows — local only, not shipped.
.github/

# Phase Gate Matrix process documents — local only, not shipped.
docs/

# Team bootstrap guide — for local session reset only.
bootstrap.md
CLAUDE.md
.gitignore
ROADMAP.md
