node_modules/
dist/
.DS_Store
*.log
.env
.env.local
*.tgz
coverage/
.vscode/
.idea/
test-results/
playwright-report/

# Pi session + audit + memory artifacts
audit.jsonl
session.jsonl
sessions/
*.sqlite
*.sqlite3
*.db

# Personal/per-machine overrides only — agents/, settings.json (team-shared) stay tracked.
.claude/settings.local.json
.claude/cache/
.claude/projects/

# Phase Gate Matrix process documents — local only, not shipped.
docs/

# Team bootstrap guide — for local session reset only.
bootstrap.md
