## Summary

<!-- One paragraph: what changed and why. -->

## Verification

- [ ] `npm run check` passes
- [ ] `npm run lint` passes
- [ ] `npm run test:fast` passes
- [ ] New behavior ships a mock test (and a live smoke where an external
      system is involved)

## Scope

<!-- Does this touch the product contract (tool inventory, prompt structure,
persistence schema, security boundary)? If yes, say so explicitly. -->
